// import Users from './Users'

const Home = () => {
  return (
    <div className="container">
      <button type="button" class="btn btn-primary">
        Create Inventory
      </button>
    </div>
  );
};

export default Home;
